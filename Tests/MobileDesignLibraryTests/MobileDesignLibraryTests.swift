import XCTest
@testable import MobileDesignLibrary

final class MobileDesignLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(MobileDesignLibrary().text, "Hello, World!")
    }
}
